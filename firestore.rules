rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global rule for app operational data
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

